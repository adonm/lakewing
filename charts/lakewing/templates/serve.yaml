apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-serve
  labels: &labels
    app.kubernetes.io/name: lakewing
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: serve
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      {{- with .Values.nodeSelector }}
      nodeSelector: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{ toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: serve
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --catalog-uri={{ .Values.serve.catalogUri }}
            - --table={{ .Values.serve.table }}
            - --tag={{ .Values.serve.tag }}
            - --listen={{ .Values.serve.listen }}
            - --concurrency={{ .Values.serve.concurrency }}
            - --duck-threads={{ .Values.serve.duckThreads }}
            - --duck-memory-mb={{ .Values.serve.duckMemoryMb }}
            - --response-cache-bytes={{ .Values.serve.responseCacheBytes }}
            {{- if .Values.serve.flightListen }}
            - --flight-listen={{ .Values.serve.flightListen }}
            {{- end }}
            {{- if .Values.serve.otelEndpoint }}
            - --otel-endpoint={{ .Values.serve.otelEndpoint }}
            {{- end }}
            {{- if .Values.serve.cacheDir }}
            - --cache-dir={{ .Values.serve.cacheDir }}
            {{- if .Values.serve.cacheBytes }}
            - --cache-bytes={{ .Values.serve.cacheBytes }}
            {{- end }}
            {{- if .Values.serve.cacheMemoryBytes }}
            - --cache-memory-bytes={{ .Values.serve.cacheMemoryBytes }}
            {{- end }}
            {{- if .Values.serve.cacheMetadataBytes }}
            - --cache-metadata-bytes={{ .Values.serve.cacheMetadataBytes }}
            {{- end }}
            - --cache-block-bytes={{ .Values.serve.cacheBlockBytes }}
            - --cache-index-block-bytes={{ .Values.serve.cacheIndexBlockBytes }}
            - --cache-align-max-blocks={{ .Values.serve.cacheAlignMaxBlocks }}
            - --cache-max-range-bytes={{ .Values.serve.cacheMaxRangeBytes }}
            {{- if .Values.serve.cacheFetchConcurrency }}
            - --cache-fetch-concurrency={{ .Values.serve.cacheFetchConcurrency }}
            {{- end }}
            {{- end }}
            {{- if .Values.serve.lanceIndexCacheBytes }}
            - --lance-index-cache-bytes={{ .Values.serve.lanceIndexCacheBytes }}
            {{- end }}
            {{- if .Values.serve.lanceMetadataCacheBytes }}
            - --lance-metadata-cache-bytes={{ .Values.serve.lanceMetadataCacheBytes }}
            {{- end }}
            {{- if .Values.serve.endpoint }}
            - --endpoint={{ .Values.serve.endpoint }}
            {{- end }}
            {{- if .Values.serve.s3KeySecret }}
            - --s3-key=$(S3_KEY)
            - --s3-secret=$(S3_SECRET)
            {{- else if not .Values.serve.endpoint }}
            # No endpoint and no credentials: cloud S3 with ambient credentials.
            {{- end }}
          {{- if or .Values.serve.s3KeySecret .Values.serve.cacheDir }}
          env:
            {{- if .Values.serve.s3KeySecret }}
            - name: S3_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.serve.s3KeySecret }}
                  key: s3-key
            - name: S3_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.serve.s3KeySecret }}
                  key: s3-secret
            {{- end }}
            {{- if .Values.serve.cacheDir }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: 3000
            {{- if .Values.serve.flightListen }}
            - name: flight
              containerPort: 50051
            {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
          volumeMounts:
            {{- if .Values.serve.cacheDir }}
            # Each pod gets its own directory under the node-local hostPath:
            # the foyer disk tier is single-process and must never be shared.
            - name: cache
              mountPath: {{ .Values.serve.cacheDir }}
              subPathExpr: $(POD_NAME)
            {{- end }}
          resources: {{ toYaml .Values.resources | nindent 12 }}
      volumes:
        {{- if .Values.serve.cacheDir }}
        - name: cache
          hostPath:
            path: {{ .Values.cacheHostPath }}
            type: DirectoryOrCreate
        {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app.kubernetes.io/name: lakewing
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  selector:
    app.kubernetes.io/name: lakewing
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: serve
  ports:
    - name: http
      port: 80
      targetPort: http
    {{- if .Values.serve.flightListen }}
    - name: flight
      port: 50051
      targetPort: flight
    {{- end }}
