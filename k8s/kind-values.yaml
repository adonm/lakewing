# kind overrides: single-zone hostPath lake, kind image tag.
image:
  tag: kind
zones:
  - az-a
replicasPerZone: 1
catalog: sha_seed.ducklake
csi:
  enabled: false
temp:
  sizeLimit: 2Gi
bulk:
  enabled: true
  replicasPerZone: 1
  connections: 4
  flightConcurrency: 2
